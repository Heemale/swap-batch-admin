import { Card, CardContent, CardHeader } from "@mui/material";

export const Dashboard = () => (
    <Card>
        <CardHeader title="swap机器人后台" />
        <CardContent>根据需要设置任务，全自动打款、授权、swap、归集。</CardContent>
    </Card>
);